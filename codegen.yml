# codegen.yml

schema: https://similar-jenny-consciously.koyeb.app/graphql
documents: './src/**/*.graphql'
generates:
  ./src/graphql/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
